/*
 * ==================================================================================
 * [파일 정보]
 * 위치  : app/lib/constants.ts
 * 역할  : 프론트 공통 상수 정의 (개월수 옵션 등)
 * 사용처 : PriceCalculator.tsx, app/accommodations/[id]/page.tsx
 * ----------------------------------------------------------------------------------
 * [기능 목록]
 * - MONTH_OPTIONS : 개월수 선택 드롭다운 옵션 배열 (1~6개월 이상)
 * ----------------------------------------------------------------------------------
 * [주의사항 / 참고]
 * - 6개월 이상은 할인율이 동일 → UI에서 하나로 통합 표시
 * - 변경 시 이 파일만 수정하면 모든 사용처에 반영됨
 * ==================================================================================
 */

// 6개월 이상은 할인율이 동일하므로 UI에서 하나로 통합
// 변경 필요 시 이 파일만 수정
export const MONTH_OPTIONS = [
  { value: 1, label: '1개월' },
  { value: 2, label: '2개월' },
  { value: 3, label: '3개월' },
  { value: 4, label: '4개월' },
  { value: 5, label: '5개월' },
  { value: 6, label: '6개월 이상' },
];

// 팀 수 선택 옵션 (1~12팀)
// 변경 필요 시 이 파일만 수정
export const TEAM_OPTIONS = Array.from({ length: 12 }, (_, i) => ({
  value: i + 1,
  label: `${i + 1} 팀`,
}));
